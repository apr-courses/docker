version: '3'

services:
  nginx:
        image: nginx:latest
        ports:
            - "4010:80"
        volumes:
            - ./src:/code
            - ./site.conf:/etc/nginx/conf.d/default.conf
        links:
            - php
            
  php:
      image: php:8-fpm
      volumes:
          - ./src:/code